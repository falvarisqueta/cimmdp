<% unless notice.blank? %>
  <div class="alert alert-success">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
    <strong><%= notice %></strong>
  </div>
<% end %>

<h1>Listado Protocolos</h1>

<table id="protocols" class="datatable">
  <thead>
    <tr>
      <th style="display:none;">Id</th>
      <th>Code</th>
      <th>Name</th>
      <th>Sponsor</th>
      <th>Patients comm</th>
      <th>PI</th>
      <th>Sub Investigador</th>
      <th>Coordinador</th>
      <th>Backup Coordinator</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @protocols.each do |protocol| %>
      <tr>
        <td style="display:none;" class="id"><%= protocol.id %></td>
        <td><%= protocol.code %></td>
        <td><%= protocol.name %></td>
        <td><%= protocol.sponsor_name %></td>
        <td><%= protocol.patients_commitment %></td>
        <td><%= protocol.principal_investigator_full_name %></td>
        <td><%= protocol.sub_investigator_full_name %></td>
        <td><%= protocol.coordinator_full_name %></td>
        <td><%= protocol.backup_coordinator_full_name %></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<a href="<%= root_path %>" class="btn btn-default">Back</a>
<button type="button" class="btn btn-primary" onclick="openDialog('protocol_modal','<%= new_protocol_path %>')">New Protocol</button>

<div id="protocol_modal" class="modal fade" role="dialog"></div>

<script>
$( document ).ready(function() {
  formatTable('protocols', 'protocol_modal', 'protocols');
});
</script>
